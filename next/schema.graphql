schema {
  query: Query
}

type Author {
  _id: Int
  firstName: String
  lastName: String
}

type Book {
  author: Author
  id: Int
  title: String
}

type Query {
  author(id: Int): Author
  book(id: Int): Book
  books: [Book]
}