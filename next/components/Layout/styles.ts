import type {CSSProperties} from "react";

export const pageBgStyle: CSSProperties = {backgroundColor: '#453643'}
export const pageContentStyle: CSSProperties = {
    display: 'flex',
    width: "100%",
    margin: '0 auto',
    maxWidth: '768px',
    flexDirection: "column",
    padding: '4px 8px',
    backgroundColor: 'rgb(253, 255, 248)',
    minHeight: '100vh'
}
